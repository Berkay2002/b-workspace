/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "images.unsplash.com",
      "befitting-weasel-639.convex.cloud",
      "lovely-eagle-414.convex.cloud"
    ],
  },
};

module.exports = nextConfig;
